<p id="notice"><%= notice %></p>
<div class="form-group">
  <label for="preco">Preço</label>
  <input type="text" class="form-control" id="preco" value="<%= @divida.preco %>" disabled="true">
</div>
<div class="form-group">
  <label for="descricao">Descrição</label>
  <input type="text" class="form-control" id="descricao" value="<%= @divida.descricao %>" disabled="true">
</div>
<div class="form-group">
  <label for="cliente">Cliente</label>
  <input type="text" class="form-control" id="cliente" value="<%= @divida.cliente.nome %>" disabled="true">
</div>

<%= link_to 'Back', dividas_path, class: "btn btn-danger" %>
<%= link_to 'Edit', edit_divida_path(@divida), class: "btn btn-primary" %>
